name: vsky
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pyqt=5
  - numpy
  - pillow
  - gdal
  # GPU acceleration cross-platform via Taichi (NVIDIA/AMD/Intel/Apple Silicon)
  - pip:
    - taichi
